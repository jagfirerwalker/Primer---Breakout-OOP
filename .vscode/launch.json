{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/breakout",
            "args": [],
            "cwd":"${workspaceRoot}",
            "console":"integratedTerminal"
        }
    ]
}